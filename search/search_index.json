{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Scalable Raster Analytics with STAC, xarray &amp; Dask","text":"<p>Satellite imagery and raster datasets are growing at an unprecedented scale. Multi-year time series, high-resolution imagery, and global coverage products now routinely exceed what can fit into memory \u2014 or even onto a laptop. Traditional GIS workflows built around downloading individual GeoTIFF files and processing them one by one simply do not scale to modern Earth Observation demands.</p> <p>Today\u2019s geospatial challenges \u2014 climate monitoring, agricultural forecasting, disaster response, urban growth analysis \u2014 require workflows that are discoverable, streamable, parallelizable, and reproducible. We need systems that can query data dynamically, process only what is necessary, scale computation across cores, and produce cloud-ready outputs.</p> <p>This guide addresses that shift. Instead of file-based raster processing, we adopt a cloud-native architecture built on open standards and scalable Python tools. By integrating STAC for discovery, Cloud-Optimized GeoTIFFs for efficient access, xarray for multi-dimensional data modeling, and Dask for parallel execution, we move from isolated raster files to scalable, analysis-ready data cubes.</p>"},{"location":"#core-concepts-workflow-components","title":"Core Concepts &amp; Workflow Components","text":"Concept Technology Role in the workflow Discovery STAC Find and filter spatiotemporal raster assets across catalogs Streaming Cloud-Optimized GeoTIFF (COG) Read only the bytes you need over HTTP Data cube xarray Multi-dimensional raster model (time, band, y, x) Geospatial rioxarray CRS, bounds, and raster I/O in xarray Scalability Dask Parallel, chunked computation that fits memory"},{"location":"#prerequisites","title":"Prerequisites","text":"<ul> <li>Python: 3.10+ recommended.</li> <li>Experience: Basic Python (functions, imports, data structures). Familiarity with NumPy or pandas is helpful but not required.</li> <li>Environment: Create a conda environment and install dependencies (Environment Setup).</li> </ul>"},{"location":"#license-attribution","title":"License &amp; attribution","text":"<p>This course uses open catalogs (e.g. Microsoft Planetary Computer) and open-source libraries. Please attribute data and software according to their respective licenses.</p>"},{"location":"intro/","title":"Introduction","text":""},{"location":"intro/#why-cloud-native-raster-analytics","title":"Why cloud-native raster analytics?","text":"<p>Traditional raster workflows often assume:</p> <ul> <li>Data lives on your machine or a mounted drive.</li> <li>You download full scenes or tiles before analysis.</li> <li>Processing is limited by RAM and single-machine CPU.</li> </ul> <p>Cloud-native workflows invert this:</p> <ul> <li>Discovery happens via catalogs (STAC) over the web.</li> <li>Data stays in the cloud; you stream only the windows or bands you need (e.g. via COGs).</li> <li>Compute can be chunked and parallelized (Dask) so that large rasters never need to be fully loaded.</li> </ul> <p>The result: you can work with continental or global, multi-temporal rasters from a laptop or a cluster using the same code.</p>"},{"location":"intro/#the-data-cube-concept","title":"The data cube concept","text":"<p>A data cube (or raster data cube) is a multi-dimensional array where dimensions typically include:</p> <ul> <li>Spatial: <code>y</code>, <code>x</code> (or lat/lon)</li> <li>Temporal: <code>time</code></li> <li>Thematic: <code>band</code>, <code>variable</code>, or <code>layer</code></li> </ul> <p>Conceptually:</p> <pre><code>         time \u2192\nband  [  [ 2D raster ]  [ 2D raster ]  ...  ]\n      [  [ 2D raster ]  [ 2D raster ]  ...  ]\n      ...\n</code></pre> <p>In this course we use xarray to represent this model: dimensions and coordinates are labeled, so you select by name (e.g. <code>time</code>, <code>band</code>) instead of axis index. That makes code readable and less error-prone when combining multiple scenes or variables.</p>"},{"location":"intro/#tooling-overview","title":"Tooling overview","text":"Layer Tool Purpose Discovery STAC (pystac-client) Search catalogs by bbox, time, and metadata Storage / transfer Cloud-Optimized GeoTIFF HTTP range requests; read windows without full download Data model xarray Labeled N-dimensional arrays (the \u201ccube\u201d) Geospatial rioxarray CRS, bounds, and GeoTIFF/COG I/O in xarray Compute Dask Lazy, chunked, parallel execution <p>Together they form a pipeline: STAC (find) \u2192 COG (stream) \u2192 xarray (model) \u2192 Dask (scale).</p>"},{"location":"notebooks/","title":"Download notebooks","text":"<p>All lesson code is included in the lesson pages in the docs. These Jupyter notebooks contain the same content so you can download and run them in your own environment.</p> Lesson File Description 1. STAC <code>01-stac-discovery.ipynb</code> Search a STAC catalog and inspect items/assets 2. COG <code>02-cog-streaming.ipynb</code> Open a COG from URL and read a window 3. xarray <code>03-xarray-datacube.ipynb</code> Build and slice a small data cube 4. rioxarray <code>04-rioxarray.ipynb</code> Open a COG with rioxarray, clip, reproject 5. Dask <code>05-dask-analytics.ipynb</code> Chunked NDVI and lazy reduction 6. Workflow <code>06-end-to-end.ipynb</code> STAC \u2192 COG \u2192 xarray \u2192 Dask pipeline <p>Notebooks are in the <code>notebooks/</code> directory. After installing dependencies, open them in your preferred notebook environment.</p>"},{"location":"references/","title":"References &amp; further reading","text":""},{"location":"references/#specifications-standards","title":"Specifications &amp; standards","text":"<ul> <li>STAC Specification \u2014 SpatioTemporal Asset Catalog.</li> <li>Cloud Optimized GeoTIFF \u2014 COG overview and best practices.</li> <li>xarray Documentation \u2014 Data structures and user guide.</li> <li>Dask Documentation \u2014 Parallel computing with blocked algorithms.</li> </ul>"},{"location":"references/#python-libraries","title":"Python libraries","text":"<ul> <li>pystac-client \u2014 STAC API client.</li> <li>rioxarray \u2014 Geospatial extension for xarray.</li> <li>rasterio \u2014 Raster I/O (used by rioxarray).</li> <li>planetary-computer \u2014 Microsoft Planetary Computer SDK and signing.</li> </ul>"},{"location":"references/#catalogs-data","title":"Catalogs &amp; data","text":"<ul> <li>Microsoft Planetary Computer \u2014 STAC API and datasets (Sentinel-2, Landsat, etc.).</li> <li>Earth Search (Element 84) \u2014 STAC API over AWS.</li> </ul>"},{"location":"setup/","title":"Environment Setup","text":"<p>This page configures an environment for the Scalable Raster Analytics workshop: STAC discovery, Cloud-Optimized GeoTIFF (COG) streaming, xarray data cubes, rioxarray, and Dask for parallel, chunked processing.</p>"},{"location":"setup/#1-install-conda-recommended","title":"1. Install Conda (Recommended)","text":"<p>Conda is a cross-platform environment manager that makes it easy to install geospatial libraries and keep the workshop stack isolated.</p>"},{"location":"setup/#windows","title":"Windows","text":"<ol> <li>Download the Miniconda installer: Miniconda Windows 64-bit</li> <li>Run the installer and choose \u201cAdd Miniconda to PATH\u201d during setup.</li> <li>Open Anaconda Prompt or Command Prompt and verify:</li> </ol> <pre><code>conda --version\n</code></pre>"},{"location":"setup/#macos","title":"macOS","text":"<ol> <li>Download the installer for your chip (Intel or Apple Silicon): Miniconda macOS</li> <li>Run the installer.</li> <li>Restart the terminal and verify:</li> </ol> <pre><code>conda --version\n</code></pre>"},{"location":"setup/#2-create-a-conda-environment","title":"2. Create a Conda Environment","text":"<p>Use a dedicated environment so the workshop stack does not conflict with other projects:</p> <pre><code>conda create --name geoenv python=3.10\nconda activate geoenv\n</code></pre>"},{"location":"setup/#3-install-the-workshop-stack","title":"3. Install the Workshop Stack","text":""},{"location":"setup/#option-a-conda-recommended","title":"Option A: Conda (recommended)","text":"<p>Install the core geospatial and scientific stack from <code>conda-forge</code>:</p> <pre><code>conda install -c conda-forge rasterio xarray rioxarray dask pystac-client \\\n  numpy geopandas notebook ipykernel\n</code></pre> <p>Optional: for Microsoft Planetary Computer signed URLs in the lessons:</p> <pre><code>pip install planetary-computer\n</code></pre>"},{"location":"setup/#option-b-pip-only","title":"Option B: Pip only","text":"<p>If you prefer not to use Conda for libraries:</p> <pre><code>pip install numpy xarray rioxarray dask pystac-client geopandas\n</code></pre>"},{"location":"setup/#4-jupyter-kernel-optional","title":"4. Jupyter Kernel (Optional)","text":"<p>To use this environment as a Jupyter kernel:</p> <pre><code>conda activate geoenv\nconda install -c conda-forge notebook ipykernel\npython -m ipykernel install --user --name=geoenv --display-name \"Python (geoenv)\"\n</code></pre> <p>In Jupyter: Kernel \u2192 Change Kernel \u2192 Python (geoenv).</p> <p>To remove the kernel later:</p> <pre><code>jupyter kernelspec uninstall geoenv\n</code></pre>"},{"location":"lessons/01-stac-discovery/","title":"STAC \u2014 Discovery","text":"<p>Goal: Use the SpatioTemporal Asset Catalog (STAC) to discover raster assets by location, time, and metadata\u2014without downloading full datasets.</p>"},{"location":"lessons/01-stac-discovery/#what-is-stac","title":"What is STAC?","text":"<p>STAC (SpatioTemporal Asset Catalog) is an open specification for describing geospatial assets (imagery, derived rasters, etc.) in a consistent way. A STAC Catalog exposes:</p> <ul> <li>Collections: groups of assets (e.g. \u201cSentinel-2 L2A\u201d, \u201cLandsat C2 L2\u201d).</li> <li>Items: individual scenes or products, each with:</li> <li>Spatial extent: <code>bbox</code> and/or <code>geometry</code>.</li> <li>Temporal extent: <code>datetime</code> or interval.</li> <li>Assets: links to actual files (e.g. COG URLs) with roles (e.g. <code>visual</code>, <code>data</code>, <code>metadata</code>).</li> </ul> <p>By querying a STAC API, you get metadata and URLs; you do not download the imagery until you open it (e.g. with rioxarray).</p>"},{"location":"lessons/01-stac-discovery/#the-stac-specification","title":"The STAC Specification","text":"<p>The STAC Specification consists of 4 semi-independent specifications. Each can be used alone, but they work best in concert with one another.</p> Specification Role STAC Item The core atomic unit: a single spatiotemporal asset represented as a GeoJSON feature plus <code>datetime</code> and <code>links</code>. STAC Catalog A simple, flexible JSON file of links that provides a structure to organize and browse STAC Items. A series of best practices helps make recommendations for creating real-world STAC Catalogs. STAC Collection An extension of the STAC Catalog with additional information\u2014extents, license, keywords, providers, etc.\u2014that describe the STAC Items that fall within the Collection. STAC API A RESTful endpoint that enables search of STAC Items, specified in OpenAPI and following OGC's WFS 3. <p>In this guide we interact mainly with a STAC API: we search for Items (often scoped by Collection), then use each Item\u2019s asset links to stream rasters. The Catalog is the entry point we open with <code>pystac_client.Client.open(...)</code>.</p> <p>Read More: STAC Specification.</p>"},{"location":"lessons/01-stac-discovery/#stac-api-and-clients","title":"STAC API and clients","text":"<p>Many providers expose a STAC API (REST endpoint). Examples:</p> <ul> <li>Microsoft Planetary Computer</li> <li>Earth Search (Element 84)</li> <li>Google Earth Engine STAC (where applicable)</li> </ul> <p>In Python, pystac-client is the standard way to search a STAC API: open the catalog, then query by bbox, datetime, and other filters.</p>"},{"location":"lessons/01-stac-discovery/#core-operations","title":"Core operations","text":""},{"location":"lessons/01-stac-discovery/#1-open-a-catalog","title":"1. Open a catalog","text":"<pre><code>import pystac_client\n\n# Planetary Computer (no API key required for read)\ncatalog = pystac_client.Client.open(\n    \"https://planetarycomputer.microsoft.com/api/stac/v1\"\n)\n</code></pre>"},{"location":"lessons/01-stac-discovery/#2-list-collections","title":"2. List collections","text":"<pre><code>for col in catalog.get_collections():\n    print(col.id)\n</code></pre>"},{"location":"lessons/01-stac-discovery/#3-search-by-bbox-and-time","title":"3. Search by bbox and time","text":"<pre><code>search = catalog.search(\n    collections=[\"sentinel-2-l2a\"],\n    bbox=[-122.5, 37.2, -122.0, 37.6],  # [min_lon, min_lat, max_lon, max_lat]\n    datetime=\"2023-06-01/2023-06-30\",\n    max_items=10,\n)\nitems = list(search.items())\n</code></pre>"},{"location":"lessons/01-stac-discovery/#4-inspect-an-item-and-its-assets","title":"4. Inspect an item and its assets","text":"<p>Each item has an <code>id</code>, <code>geometry</code>, <code>bbox</code>, <code>datetime</code>, and <code>assets</code> (a dict of asset keys \u2192 asset objects). Each asset has <code>href</code> (URL), optional <code>title</code>, and <code>roles</code>.</p> <pre><code>item = items[0]\nprint(item.id, item.datetime)\nfor key, asset in item.assets.items():\n    print(key, asset.href)\n</code></pre> <p>The <code>href</code> for a COG is what you will pass to rioxarray or rasterio to open and stream the raster.</p>"},{"location":"lessons/01-stac-discovery/#signing-urls-planetary-computer","title":"Signing URLs (Planetary Computer)","text":"<p>Microsoft Planetary Computer uses signed URLs so that access is time-limited. Use the <code>planetary_computer</code> package to sign asset URLs:</p> <pre><code>import planetary_computer\n\nfor item in items:\n    item = planetary_computer.sign(item)\n# Now item.assets[\"...\"].href are signed and can be used by rioxarray\n</code></pre>"},{"location":"lessons/01-stac-discovery/#best-practices","title":"Best practices","text":"<ul> <li>Filter early: Use <code>bbox</code>, <code>datetime</code>, and <code>query</code> (e.g. cloud cover) to keep the result set small.</li> <li>Respect limits: Use <code>max_items</code> or pagination to avoid pulling thousands of items.</li> <li>Reuse signed items: Sign once, then open multiple assets from the same item.</li> <li>Check licenses: Catalogs and collections document license and attribution requirements.</li> </ul>"},{"location":"lessons/01-stac-discovery/#summary","title":"Summary","text":"Concept Detail STAC Catalog of geospatial assets with bbox, time, and asset links STAC API HTTP API to search collections/items pystac-client Python client to open catalog and run <code>.search()</code> Item \u2192 assets Each item has <code>assets</code>; each asset has <code>href</code> (e.g. COG URL) Signing For PC, use <code>planetary_computer.sign(item)</code> before using <code>href</code> <p>In next lesson we use those asset URLs to open rasters as Cloud-Optimized GeoTIFFs and stream only the data we need.</p> <p>Download the notebook for this lesson: <code>notebooks/01-stac-discovery.ipynb</code>.</p>"},{"location":"lessons/02-cog-streaming/","title":"Cloud-Optimized GeoTIFF (COG)","text":"<p>Goal: Understand how Cloud-Optimized GeoTIFFs enable streaming and windowed reads over HTTP, so you only transfer the bytes you need.</p>"},{"location":"lessons/02-cog-streaming/#what-is-a-cloud-optimized-geotiff","title":"What is a Cloud-Optimized GeoTIFF?","text":"<p>A GeoTIFF is a TIFF file with georeferencing (CRS, extent, resolution) in the same container. A Cloud-Optimized GeoTIFF (COG) is a GeoTIFF organized so that:</p> <ol> <li>Tiling: The image is stored in small tiles (e.g. 512\u00d7512).</li> <li>Overview pyramids: Lower-resolution overviews are embedded.</li> <li>Metadata and layout: Key metadata and the tile index are near the start of the file.</li> </ol> <p>When the file is served over HTTP (e.g. from S3 or Azure Blob), a client can issue HTTP range requests to read only specific byte ranges. So instead of downloading a 2 GB scene, you can read a single tile or a small window\u2014streaming only what you need.</p>"},{"location":"lessons/02-cog-streaming/#why-this-matters-for-analytics","title":"Why this matters for analytics","text":"<ul> <li>Band selection: Read only the bands you need.</li> <li>Spatial windowing: Read a bounding box (e.g. your study area) instead of the full scene.</li> <li>Overview levels: For quick previews or coarse analysis, read from an overview.</li> <li>Lazy loading: Libraries like rasterio and rioxarray can open the URL and only fetch data when you actually read a window or trigger computation.</li> </ul>"},{"location":"lessons/02-cog-streaming/#how-reading-a-cog-works-conceptually","title":"How reading a COG works (conceptually)","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  COG on HTTP (e.g. S3 / Azure)                          \u2502\n\u2502  \u250c\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2510                                    \u2502\n\u2502  \u2502Tile \u2502Tile \u2502Tile \u2502  ...                               \u2502\n\u2502  \u251c\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2524                                    \u2502\n\u2502  \u2502Tile \u2502Tile \u2502Tile \u2502  Client requests range(bytes)     \u2502\n\u2502  \u2514\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2518       \u2192 only those tiles transfer  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>The client (rasterio/rioxarray) uses the file\u2019s internal structure to map a window (in pixel or geographic coordinates) to byte ranges, then requests those ranges. No special \u201cCOG protocol\u201d is needed\u2014just HTTP Range.</p>"},{"location":"lessons/02-cog-streaming/#opening-a-cog-from-a-url","title":"Opening a COG from a URL","text":"<p>With rasterio, you open the URL as if it were a path. The driver uses the appropriate backend (e.g. GDAL\u2019s <code>/vsicurl/</code> or cloud-specific handlers) to perform range requests.</p> <p>Get a COG URL from STAC (e.g. signed Planetary Computer URL):</p> <pre><code>import pystac_client\nimport planetary_computer\n\ncatalog = pystac_client.Client.open(\n    \"https://planetarycomputer.microsoft.com/api/stac/v1\"\n)\nsearch = catalog.search(\n    collections=[\"sentinel-2-l2a\"],\n    bbox=[-122.4, 37.6, -122.2, 37.8],\n    datetime=\"2023-06-15\",\n    max_items=1,\n)\nitem = planetary_computer.sign(list(search.items())[0])\nurl = item.assets[\"B04\"].href\n</code></pre> <p>Open the COG and read metadata (no full download):</p> <pre><code>import rasterio\nfrom rasterio.windows import from_bounds\n\nwith rasterio.open(url) as src:\n    print(\"CRS:\", src.crs)\n    print(\"Bounds:\", src.bounds)\n    print(\"Shape (height, width):\", src.shape)\n    print(\"Block size:\", src.block_shapes)\n</code></pre> <p>Read a window by geographic bounds (only those tiles are streamed):</p> <pre><code>left, bottom, right, top = -122.35, 37.65, -122.30, 37.70\n\nwith rasterio.open(url) as src:\n    window = from_bounds(left, bottom, right, top, src.transform)\n    data = src.read(1, window=window)\n    print(\"Window shape:\", data.shape)\n</code></pre> <p>Optional: plot the window</p> <pre><code>import matplotlib.pyplot as plt\n\nwith rasterio.open(url) as src:\n    window = from_bounds(left, bottom, right, top, src.transform)\n    data = src.read(1, window=window)\n\nfig, ax = plt.subplots(1, 1, figsize=(6, 5))\nax.imshow(data, cmap=\"gray\")\nax.set_title(\"COG window (B04) \u2014 streamed only\")\nplt.tight_layout()\nplt.show()\n</code></pre> <p>With rioxarray, you will do the same conceptually: <code>rioxarray.open_rasterio(url)</code> and then slice or use <code>.sel()</code> so that only the required window is read.</p>"},{"location":"lessons/02-cog-streaming/#requirements-for-a-true-cog","title":"Requirements for a \u201ctrue\u201d COG","text":"<p>For efficient streaming, the file should:</p> <ul> <li>Be tiled (not strip-based).</li> <li>Have overview levels if you want fast coarse reads.</li> <li>Be served from a store that supports HTTP Range (S3, Azure Blob, and most HTTP servers do).</li> </ul> <p>You can check with <code>gdalinfo</code> or rasterio\u2019s <code>src.profile</code> (e.g. <code>blockxsize</code>, <code>blockysize</code>).</p>"},{"location":"lessons/02-cog-streaming/#summary","title":"Summary","text":"Concept Detail COG GeoTIFF with tiling + overviews, optimized for HTTP range requests Streaming Client requests only the byte ranges for the window/bands needed Benefit No full-file download; band and spatial subsetting over the network In Python rasterio / rioxarray open URL; reads trigger range requests <p>Download the notebook for this lesson: <code>notebooks/02-cog-streaming.ipynb</code>.</p>"},{"location":"lessons/03-xarray-datacube/","title":"xarray &amp; the Data Cube","text":"<p>Goal: Model multi-dimensional rasters as labeled arrays with xarray: dimensions (e.g. time, band, y, x), coordinates, and attributes.</p>"},{"location":"lessons/03-xarray-datacube/#from-raw-arrays-to-a-data-cube","title":"From raw arrays to a data cube","text":"<p>A single-band 2D raster is just a 2D array (e.g. NumPy). As soon as you have:</p> <ul> <li>Multiple bands (e.g. red, green, blue, NIR),</li> <li>Multiple times (e.g. monthly composites),</li> <li>Multiple variables (e.g. reflectance and a mask),</li> </ul> <p>you have a multi-dimensional raster. The data cube is the conceptual model: a cube (or hypercube) where each axis is a dimension and each point has a label (e.g. time, band name).</p> <p>xarray implements this with:</p> <ul> <li>Dimensions: named axes (e.g. <code>time</code>, <code>band</code>, <code>y</code>, <code>x</code>).</li> <li>Coordinates: arrays that label each point along a dimension (e.g. dates for <code>time</code>, band names for <code>band</code>).</li> <li>Data variables: arrays that depend on those dimensions.</li> <li>Attributes: metadata (e.g. CRS, units) on the dataset or variables.</li> </ul> <p> Source: Stephan Hoyer, ECMWF presentation. Link to original talk</p>"},{"location":"lessons/03-xarray-datacube/#core-structures","title":"Core structures","text":""},{"location":"lessons/03-xarray-datacube/#dataarray","title":"DataArray","text":"<p>A DataArray is a single array with dimensions and coordinates:</p> <pre><code>import xarray as xr\nimport numpy as np\n\n# Minimal example: 2D (y, x)\ndata = np.random.rand(100, 200)\nda = xr.DataArray(\n    data,\n    dims=[\"y\", \"x\"],\n    coords={\n        \"y\": np.linspace(4000, 3000, 100),  # e.g. northing\n        \"x\": np.linspace(1000, 3000, 200),  # e.g. easting\n    },\n    attrs={\"units\": \"m\", \"long_name\": \"elevation\"},\n)\nda.sel(x=1500, method=\"nearest\")\n</code></pre>"},{"location":"lessons/03-xarray-datacube/#dataset","title":"Dataset","text":"<p>A Dataset is a dict-like container of DataArrays that share dimensions. Typical for rasters: one variable per band or one variable per product, all sharing <code>y</code> and <code>x</code> (and optionally <code>time</code>, <code>band</code>).</p> <pre><code>ds = xr.Dataset(\n    {\n        \"red\": ([\"y\", \"x\"], red_array),\n        \"nir\": ([\"y\", \"x\"], nir_array),\n    },\n    coords={\"y\": y_coord, \"x\": x_coord},\n)\nds[\"ndvi\"] = (ds[\"nir\"] - ds[\"red\"]) / (ds[\"nir\"] + ds[\"red\"])\n</code></pre>"},{"location":"lessons/03-xarray-datacube/#why-labels-matter","title":"Why labels matter","text":"<p>With NumPy, you do <code>arr[i, j]</code> or <code>arr[0:10, :]</code> and must remember which axis is time or band. With xarray you do:</p> <pre><code>ds.sel(time=\"2023-06-15\", band=\"red\")\nds.sel(x=slice(1000, 2000), y=slice(3000, 4000))\nds.isel(time=0)\n</code></pre> <p>That makes code readable and less error-prone when combining multiple scenes or variables.</p>"},{"location":"lessons/03-xarray-datacube/#dimensions-typical-in-raster-cubes","title":"Dimensions typical in raster cubes","text":"Dimension Meaning Example coordinates <code>x</code> Horizontal (easting / lon) Projected x or longitude <code>y</code> Vertical (northing / lat) Projected y or latitude <code>band</code> Spectral/thematic layer <code>[\"red\", \"green\", \"blue\", \"nir\"]</code> <code>time</code> Time step <code>datetime64</code> dates <p>So a multi-temporal, multi-band stack might have shape <code>(time, band, y, x)</code>.</p>"},{"location":"lessons/03-xarray-datacube/#lazy-vs-eager","title":"Lazy vs eager","text":"<p>xarray can wrap Dask arrays instead of NumPy. Then the array is lazy: operations build a task graph; actual reads and compute happen when you call <code>.compute()</code> or when you trigger plotting. We use this in Lesson 5 for scalable analytics; in Lesson 4 we open COGs as xarray (with rioxarray) and can make them lazy by default.</p>"},{"location":"lessons/03-xarray-datacube/#summary","title":"Summary","text":"Concept Detail Data cube Multi-dimensional raster with labeled dimensions (time, band, y, x) DataArray Single array + dims + coords + attrs Dataset Multiple DataArrays sharing dimensions Selection <code>.sel()</code> by coordinate, <code>.isel()</code> by index, <code>.slice()</code> Lazy xarray can hold Dask arrays; compute on demand <p>In next lesson we use rioxarray to open COGs as xarray objects with CRS and spatial coordinates.</p> <p>Download the notebook for this lesson: <code>notebooks/03-xarray-datacube.ipynb</code>.</p>"},{"location":"lessons/04-rioxarray/","title":"rioxarray \u2014 Geospatial xarray","text":"<p>Goal: Open Cloud-Optimized GeoTIFFs (and other rasters) as xarray objects with correct CRS, bounds, and dimensions using rioxarray.</p>"},{"location":"lessons/04-rioxarray/#what-is-rioxarray","title":"What is rioxarray?","text":"<p>rioxarray extends xarray so that raster I/O and spatial metadata are first-class:</p> <ul> <li>Open rasters (GeoTIFF, COG, etc.) from path or URL into a <code>DataArray</code> or <code>Dataset</code>.</li> <li>Attach CRS and transform via the <code>.rio</code> accessor.</li> <li>Reproject, clip, merge, and write rasters using the same accessor.</li> </ul> <p>Under the hood it uses rasterio for reading/writing and GDAL for formats; the result is an xarray structure you can combine with Dask and the rest of the scientific Python stack.</p>"},{"location":"lessons/04-rioxarray/#opening-a-cog","title":"Opening a COG","text":"<p>Use a signed STAC asset URL (see Lesson 1 for search + sign). Example:</p> <pre><code>import rioxarray\nimport pystac_client\nimport planetary_computer\n\ncatalog = pystac_client.Client.open(\n    \"https://planetarycomputer.microsoft.com/api/stac/v1\"\n)\nsearch = catalog.search(\n    collections=[\"sentinel-2-l2a\"],\n    bbox=[-122.4, 37.6, -122.2, 37.8],\n    datetime=\"2023-06-15\",\n    max_items=1,\n)\nitem = planetary_computer.sign(list(search.items())[0])\nurl = item.assets[\"B04\"].href\n\ndata = rioxarray.open_rasterio(url)\n</code></pre> <p>Default behavior:</p> <ul> <li>Dimensions: often <code>band</code>, <code>y</code>, <code>x</code> (band as first dimension).</li> <li>Coordinates: <code>x</code> and <code>y</code> come from the geotransform (centers or edges, depending on driver).</li> <li>CRS: stored in <code>data.rio.crs</code>.</li> <li>Bounds: <code>data.rio.bounds</code>.</li> <li>Transform: <code>data.rio.transform()</code>.</li> </ul> <p>So you get a data cube slice (one scene, one or more bands) with spatial labels.</p>"},{"location":"lessons/04-rioxarray/#the-rio-accessor","title":"The <code>.rio</code> accessor","text":"<p>All spatial operations live under the <code>.rio</code> accessor:</p> Method / property Purpose <code>.rio.crs</code> Coordinate reference system <code>.rio.bounds</code> (left, bottom, right, top) <code>.rio.transform()</code> Affine transform (pixel \u2194 geo) <code>.rio.reproject()</code> Reproject to another CRS <code>.rio.clip()</code> Clip with a geometry (e.g. GeoDataFrame) <code>.rio.write_crs()</code> Set CRS before writing <code>.rio.to_raster()</code> Write to GeoTIFF/COG <p>Example: clip to a bounding box (same CRS as data), or clip with a vector.</p> <pre><code># Clip to a box\nclipped = data.rio.clip_box(minx=-122.35, miny=37.65, maxx=-122.28, maxy=37.72)\n# Or clip with a GeoDataFrame\n# clipped = data.rio.clip(geodf.geometry, geodf.crs)\n</code></pre> <p>Single-band rasters: drop the band dimension for simpler indexing: <code>data = data.squeeze(\"band\", drop=True)</code>.</p>"},{"location":"lessons/04-rioxarray/#chunked-lazy-opening","title":"Chunked (lazy) opening","text":"<p>For large rasters and Dask-based workflows, open with chunks so that the underlying array is a Dask array:</p> <pre><code>data = rioxarray.open_rasterio(url, chunks=\"auto\")  # or chunks={\"x\": 1024, \"y\": 1024}\n</code></pre> <p>Then operations are lazy until you call <code>.compute()</code> or trigger plotting. This is the bridge to Lesson 5 (Dask).</p>"},{"location":"lessons/04-rioxarray/#multi-band-and-multi-scene","title":"Multi-band and multi-scene","text":"<ul> <li>Single COG, multiple bands: <code>open_rasterio</code> returns one DataArray with dim <code>band</code>; you can <code>ds = data.to_dataset(dim=\"band\")</code> to get a Dataset with one variable per band.</li> <li>Multiple scenes (e.g. from STAC): Open each asset with <code>open_rasterio</code>, optionally align with <code>xr.concat(..., dim=\"time\")</code> and assign <code>time</code> coordinates from STAC item dates.</li> </ul> <p>We combine this in Lesson 6 for a full STAC \u2192 COG \u2192 xarray \u2192 Dask pipeline.</p>"},{"location":"lessons/04-rioxarray/#summary","title":"Summary","text":"Concept Detail rioxarray Geospatial extension for xarray: open rasters, CRS, bounds, clip, reproject Opening <code>rioxarray.open_rasterio(path_or_url)</code> \u2192 DataArray with <code>band</code>, <code>y</code>, <code>x</code> .rio Accessor for CRS, bounds, transform, clip, reproject, to_raster Chunks <code>chunks=\"auto\"</code> or explicit for Dask-backed lazy reading <p>Next we use Dask to run parallel, chunked analytics on these structures.</p> <p>Download the notebook for this lesson: <code>notebooks/04-rioxarray.ipynb</code>.</p>"},{"location":"lessons/05-dask-analytics/","title":"Dask \u2014 Scalable Raster Analytics","text":"<p>Goal: Use Dask with xarray/rioxarray to run chunked, lazy, and parallel raster analytics so that large rasters never need to be fully loaded into memory.</p>"},{"location":"lessons/05-dask-analytics/#the-scalability-problem","title":"The scalability problem","text":"<ul> <li>A single Sentinel-2 tile might be 10+ GB (all bands, full resolution).</li> <li>A time series of 100 dates exceeds RAM on most machines if loaded at once.</li> <li>Solution: Keep data in chunks, compute only the chunks that are needed, and run operations in parallel over chunks. Dask does exactly that.</li> </ul>"},{"location":"lessons/05-dask-analytics/#dask-in-one-paragraph","title":"Dask in one paragraph","text":"<p>Dask provides array-like types (<code>dask.array</code>) that look like NumPy but:</p> <ul> <li>Are lazy: operations build a task graph instead of executing immediately.</li> <li>Are chunked: the logical array is split into blocks; each block is a task.</li> <li>Execute in parallel when you call <code>.compute()</code> (using threads, processes, or a cluster).</li> </ul> <p>xarray integrates Dask: if a DataArray\u2019s data is a <code>dask.array</code>, then xarray operations (slicing, reduction, arithmetic) stay lazy until you call <code>.compute()</code> or trigger I/O (e.g. plot, write).</p>"},{"location":"lessons/05-dask-analytics/#chunking-strategy","title":"Chunking strategy","text":"<ul> <li>Chunk size: Small enough to fit in memory (e.g. 512\u00d7512 or 1024\u00d71024 pixels per chunk); large enough to amortize overhead.</li> <li>Alignment: Chunk boundaries should align with the raster\u2019s internal blocks (e.g. COG tiles) when possible, so one read = one chunk.</li> <li>Dimensions: For a <code>(time, band, y, x)</code> cube, you might chunk in <code>y</code> and <code>x</code> and keep <code>time</code> and <code>band</code> unchunked (or chunk time if you have many dates).</li> </ul> <p>Typical pattern:</p> <pre><code># When opening with rioxarray\ndata = rioxarray.open_rasterio(url, chunks={\"x\": 1024, \"y\": 1024})\n# Or rechunk after open\ndata = data.chunk({\"x\": 1024, \"y\": 1024})\n</code></pre>"},{"location":"lessons/05-dask-analytics/#lazy-pipeline-example","title":"Lazy pipeline example","text":"<pre><code>import rioxarray\nimport xarray as xr\n\nurl = \"https://...\"\nds = rioxarray.open_rasterio(url, chunks=\"auto\").to_dataset(dim=\"band\")\nds = ds.rename({1: \"red\", 2: \"green\", 3: \"blue\", 4: \"nir\"})\n\n# All of this is lazy\nndvi = (ds[\"nir\"] - ds[\"red\"]) / (ds[\"nir\"] + ds[\"red\"])\nmean_ndvi = ndvi.mean(dim=[\"x\", \"y\"])\n\n# Only this triggers actual read + compute\nresult = mean_ndvi.compute()\n</code></pre> <p>Dask will:</p> <ol> <li>Read only the chunks needed for the requested window or reduction.</li> <li>Run arithmetic and reduction in parallel over chunks.</li> <li>Return a small result (e.g. a scalar or 1D array) without ever holding the full raster in memory.</li> </ol>"},{"location":"lessons/05-dask-analytics/#when-compute-runs","title":"When compute runs","text":"<ul> <li>Explicit: <code>.compute()</code> returns a concrete NumPy/xarray result.</li> <li>Implicit: <code>.plot()</code>, <code>.to_netcdf()</code>, <code>.rio.to_raster()</code> trigger computation for the data they need.</li> <li>Persist: <code>.persist()</code> computes and keeps the result in distributed memory (useful in a cluster) so later steps don\u2019t recompute.</li> </ul>"},{"location":"lessons/05-dask-analytics/#local-vs-distributed","title":"Local vs distributed","text":"<ul> <li>Default (threaded): <code>dask.array</code> uses a thread pool; good for I/O-bound COG reads.</li> <li>Processes: <code>dask.distributed.Client(processes=True)</code> for CPU-bound work.</li> <li>Cluster: Point xarray/Dask to a <code>distributed.Client(\"scheduler-address\")</code> to run on multiple machines. The same code (open COG, chunk, reduce) scales up.</li> </ul>"},{"location":"lessons/05-dask-analytics/#best-practices","title":"Best practices","text":"<ul> <li>Chunk to align with COG tiles when possible (e.g. 512 or 1024).</li> <li>Avoid too many tiny chunks (overhead) and too few huge chunks (memory).</li> <li>Use reductions (<code>.mean()</code>, <code>.sum()</code>) along dimensions to shrink data before <code>.compute()</code>.</li> <li>Profile: Use <code>dask.distributed.dashboard</code> or Dask\u2019s profilers to see where time is spent.</li> </ul>"},{"location":"lessons/05-dask-analytics/#summary","title":"Summary","text":"Concept Detail Dask Lazy, chunked, parallel arrays; xarray can wrap Dask arrays Chunking Split (e.g. y, x) into blocks; align with COG tiles when possible Lazy Operations build a graph; <code>.compute()</code> or I/O triggers execution Scale Same code can run on one machine or a Dask cluster <p>Download the notebook for this lesson: <code>notebooks/05-dask-analytics.ipynb</code>.</p>"},{"location":"lessons/06-end-to-end/","title":"End-to-End Workflow","text":"<p>Goal: Combine STAC (discovery), COG (streaming), xarray (data cube), rioxarray (geospatial I/O), and Dask (scalable compute) into one reproducible pipeline.</p>"},{"location":"lessons/06-end-to-end/#pipeline-overview","title":"Pipeline overview","text":"<pre><code>STAC API (search)  \u2192  Items + asset URLs  \u2192  Sign (if needed)\n       \u2192  open_rasterio(url, chunks)  \u2192  xarray Dataset\n       \u2192  Derive index (e.g. NDVI)  \u2192  Reduce (mean, std, etc.) or write\n       \u2192  .compute() / .to_raster()\n</code></pre> <p>All steps stay in Python; data stays in the cloud until you explicitly read or write.</p>"},{"location":"lessons/06-end-to-end/#step-1-discover-assets-stac","title":"Step 1: Discover assets (STAC)","text":"<pre><code>import pystac_client\nimport planetary_computer\n\ncatalog = pystac_client.Client.open(\n    \"https://planetarycomputer.microsoft.com/api/stac/v1\"\n)\nsearch = catalog.search(\n    collections=[\"sentinel-2-l2a\"],\n    bbox=[-122.4, 37.6, -122.2, 37.8],\n    datetime=\"2023-06-01/2023-06-30\",\n    max_items=5,\n)\nitems = list(search.items())\nitems = [planetary_computer.sign(item) for item in items]\n</code></pre>"},{"location":"lessons/06-end-to-end/#step-2-open-as-chunked-xarray-cog-rioxarray","title":"Step 2: Open as chunked xarray (COG + rioxarray)","text":"<p>Pick one item and open one or more bands (e.g. red and NIR for NDVI). Use chunks so the array is Dask-backed.</p> <pre><code>import rioxarray\nimport xarray as xr\n\ndef open_bands(item, band_names, chunks=None):\n    bands = {}\n    for name in band_names:\n        asset = item.assets[name]\n        da = rioxarray.open_rasterio(asset.href, chunks=chunks or \"auto\")\n        bands[name] = da.squeeze(\"band\", drop=True)\n    ds = xr.Dataset(bands)\n    ds[\"time\"] = item.datetime\n    return ds\n\n# One scene\nitem = items[0]\nds = open_bands(item, [\"B04\", \"B08\"], chunks={\"x\": 1024, \"y\": 1024})\n</code></pre>"},{"location":"lessons/06-end-to-end/#step-3-derive-an-index-xarray","title":"Step 3: Derive an index (xarray)","text":"<pre><code>ds[\"ndvi\"] = (ds[\"B08\"] - ds[\"B04\"]) / (ds[\"B08\"] + ds[\"B04\"])\n</code></pre>"},{"location":"lessons/06-end-to-end/#step-4-reduce-or-subset-dask","title":"Step 4: Reduce or subset (Dask)","text":"<ul> <li>Spatial mean (one value per scene):</li> </ul> <pre><code>mean_ndvi = ds[\"ndvi\"].mean(dim=[\"x\", \"y\"])\nmean_ndvi.compute()\n</code></pre> <ul> <li>Time series: Concatenate multiple scenes along <code>time</code>, then reduce:</li> </ul> <pre><code>scenes = [open_bands(it, [\"B04\", \"B08\"], chunks={\"x\": 1024, \"y\": 1024}) for it in items]\ncombined = xr.concat(scenes, dim=\"time\")\ncombined[\"ndvi\"] = (combined[\"B08\"] - combined[\"B04\"]) / (combined[\"B08\"] + combined[\"B04\"])\nts = combined[\"ndvi\"].mean(dim=[\"x\", \"y\"])\nts.compute()\n</code></pre>"},{"location":"lessons/06-end-to-end/#step-5-optional-write-outputs","title":"Step 5: Optional \u2014 write outputs","text":"<ul> <li>To raster: <code>combined[\"ndvi\"].rio.to_raster(\"ndvi.tif\")</code> (triggers compute for that variable).</li> <li>To NetCDF: <code>combined.to_netcdf(\"cube.nc\")</code> (with engine and encoding as needed).</li> </ul>"},{"location":"lessons/06-end-to-end/#putting-it-together","title":"Putting it together","text":"<p>A minimal end-to-end script:</p> <ol> <li>Search STAC (bbox, time, collection).</li> <li>Sign items if required.</li> <li>Open selected bands with <code>rioxarray.open_rasterio(..., chunks=...)</code>.</li> <li>Build a Dataset (and optionally concatenate over time).</li> <li>Compute derived variables (NDVI, EVI, etc.).</li> <li>Reduce (e.g. mean over space) or write (GeoTIFF/NetCDF).</li> <li>Call <code>.compute()</code> or use plotting/writing to run the graph.</li> </ol> <p>This is the scalable raster analytics workflow: discovery (STAC), streaming (COG), modeling (xarray/rioxarray), and scalable compute (Dask).</p>"},{"location":"lessons/06-end-to-end/#summary","title":"Summary","text":"Step Tool Action 1 STAC (pystac-client) Search by bbox, time; get asset URLs 2 planetary_computer (if PC) Sign asset URLs 3 rioxarray open_rasterio(url, chunks) \u2192 xarray 4 xarray Derive indices, concat over time 5 Dask (via xarray) Lazy reductions; .compute() or write to run <p>Download the notebook for this lesson: <code>notebooks/06-end-to-end.ipynb</code>.</p>"}]}